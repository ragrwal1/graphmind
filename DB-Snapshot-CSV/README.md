# Local CSV snapshots (not in Git)

Place Airtable exports here for `npm run preseed:members`. The script reads:

- `RealmSpark People Members.csv`

`*.csv` files in this directory are gitignored so member and deal data stays off GitHub.
